-b <new_branch>
           Create a new branch named <new_branch> and start it at
           <start_point>; see git-branch(1) for details.

-b <new_branch>
           Создайте новую ветку с именем <new_branch> и запустите ее
           <Start_point>; подробнее см. git-branch (1).

